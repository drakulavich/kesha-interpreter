# GPU server hostname or IP
GPU_HOST=10.119.62.29

# Riva gRPC ports (per service)
RIVA_ASR_PORT=50055
RIVA_NMT_PORT=50051
RIVA_TTS_PORT=50056

# TLS (set to 1 if endpoints require SSL)
RIVA_TLS=0

# NGC API key (only if endpoints require auth)
# RIVA_API_KEY=nvapi-xxx

# TTS voice (Magpie multilingual)
RIVA_VOICE=Magpie-Multilingual.EN-US.Leo
